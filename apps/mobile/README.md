# @bouquet/mobile

Expo host app (iOS + Android). Event setup, moderation, dashboard, Bloom config.

Guests never use this app — guest upload lives on the web surface. See `PLAN.md`.

Dependencies are declared but not installed in this repo snapshot; run
`pnpm install --filter @bouquet/mobile` then `pnpm dev` with a simulator attached.
